# Python
__pycache__/
*.pyc

# Experiment logs (regenerable; results live in the jsonl files)
*.log

# Trained checkpoints / run dirs (too large for git; retrain with the scripts)
transfer_runs/
protection_runs/
qa_sweep_runs/
qa_multi_runs/

# Zone.Identifier (Windows download artifacts)
*:Zone.Identifier:$DATA

# LaTeX build artifacts (keep main.tex/main.pdf/figs)
state_tokens/paper/*.aux
state_tokens/paper/*.fls
state_tokens/paper/*.fdb_latexmk
state_tokens/paper/*.out
state_tokens/paper/*.log
